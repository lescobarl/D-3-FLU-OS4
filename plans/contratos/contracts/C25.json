{
  "id": "C25",
  "mode": "full",
  "task": "Voz §9.2 — App no re-normaliza la frase canónica",
  "base": "__BASE__",
  "objective": "App deja de re-normalizar la transcripción; la normalización vive en el motor de voz.",
  "allow": ["src/App.tsx"],
  "deny": [".task/**", "scripts/task-gate.mjs", "scripts/audit-metric.mjs"],
  "ignore": [".task/**", "scripts/task-gate*.mjs", "scripts/audit-metric.mjs", "kilo.json"],
  "maxFiles": 2,
  "maxLines": 250,
  "dod": "npx vitest run tests/appNormalizaGuard.test.ts --reporter=dot && npm run typecheck",
  "guard": "npx vitest run tests/appNormalizaGuard.test.ts --reporter=dot",
  "guardExpect": "green",
  "invariant": {
    "statement": "App no llama normalizadores sobre la frase canónica",
    "metric": { "command": "node scripts/audit-metric.mjs app-normaliza", "target": 0 },
    "base": "__BASE__",
    "guardFiles": ["tests/appNormalizaGuard.test.ts"]
  }
}
