{
  "id": "P7.3",
  "mode": "full",
  "task": "P7.3",
  "base": "a4336b7060fd0109cc7cf56466f80aecb31beed0",
  "objective": "TS estricto: desaparece el ultimo doble cast (as unknown as) de src y su guard (C49) entra en la barrera, para que no vuelva en silencio.",
  "allow": [
    "src/core/db/fluDatabase.ts",
    "tests/**",
    "package.json",
    ".task/frozen.json",
    ".task/contract.json",
    ".task/report",
    "plans/ledger.json"
  ],
  "ignore": [
    "scripts/task-gate*.mjs",
    "kilo.json"
  ],
  "maxFiles": 12,
  "maxLines": 300,
  "dod": "npx tsc -b && npx vitest run tests/tsStrictGuard.test.ts tests/dexieSingleSourceGuard.test.ts tests/guardMutationGuard.test.ts --reporter=dot",
  "guard": "npx vitest run tests/tsStrictGuard.test.ts --reporter=dot",
  "guardExpect": "green",
  "invariant": {
    "statement": "Doble cast (as unknown as) en src: 1 -> 0.",
    "metric": {
      "command": "node -e \"const fs=require('fs'),path=require('path');const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):(/[.](ts|tsx|js|jsx|mjs)$/.test(e.name)?[path.join(d,e.name)]:[]));let n=0;for(const f of walk('src')){for(const line of fs.readFileSync(f,'utf8').split(/\\r?\\n/)){const t=line.trim();if(t.startsWith('*')||t.startsWith('/*')||t.startsWith('//'))continue;if(/as unknown as/.test(line))n++;}}console.log(n)\"",
      "target": 0
    },
    "guardFiles": [
      "tests/tsStrictGuard.test.ts"
    ]
  }
}
