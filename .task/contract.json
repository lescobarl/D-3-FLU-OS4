{
  "id": "P4.2",
  "mode": "full",
  "task": "P4.2 (a) banco del bloqueo del parse FBX + veredicto medido de (b) Three en bundle",
  "base": "2a0b41749d8073e7a4a2f0cd539fe50113f7e29f",
  "objective": "Cerrar la parte (a) con un banco EJECUTABLE (no un test de CI: vitest solo recoge tests/**/*.test.ts) que mide el bloqueo del parse sincrono en el hilo principal por la ruta real (fbxParse.ts, dueno unico). Cambio aditivo: no elimina duplicacion nueva, por eso no lleva invariante; el invariante de la unificacion 2->1 de P4.2 ya lo puso el commit f99661f con su metrica base=2 actual=1. La parte (b) queda como VEREDICTO medido en el mensaje y en el ledger: three instalada es UNA copia, three es external en el build (no hay bundle que partir) y el build no arranca por un defecto preexistente que se registra como P6.13.",
  "allow": [
    "tests/bench/**",
    ".task/**"
  ],
  "ignore": [
    "scripts/task-gate*.mjs",
    "kilo.json"
  ],
  "maxFiles": 4,
  "maxLines": 130,
  "dod": "npx vitest bench tests/bench/fbxParse.bench.ts",
  "guard": "npx vitest run tests/fbxSingleOrchestrationGuard.test.ts --reporter=dot",
  "guardExpect": "green"
}
