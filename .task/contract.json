{
  "id": "barrier",
  "mode": "full",
  "task": "hallazgo - el guard del ledger hacia 115 git cat-file y flaqueaba por timeout",
  "base": "3d25a4de2fb159c66b979459fc5dcfc97664d49d",
  "objective": "Hacer determinista la barrera: commitExists resolvia UN `git cat-file` por hash del ledger (~115 procesos) y con la suite completa en carga el guard del ledger superaba los 15 s de timeout y se ponia rojo de forma intermitente con el ledger correcto. Pasa a un solo `git cat-file --batch-check` cacheado, misma semantica. Medido: 6.0 s -> 0.13 s en el guard.",
  "allow": [
    "scripts/**",
    ".task/**",
    "src/**",
    "tests/**"
  ],
  "ignore": [
    "scripts/task-gate*.mjs",
    "kilo.json"
  ],
  "maxFiles": 10,
  "maxLines": 1000,
  "dod": "node scripts/task-ledger.mjs check",
  "guard": "npx vitest run tests/taskLedgerGuard.test.ts tests/decisionVerdictGuard.test.ts --reporter=dot",
  "guardExpect": "green"
}
